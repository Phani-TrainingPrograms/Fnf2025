//If UR Class contains only data and does not have any functions, then we can create them as interface as it does not have any body in it. 
export interface Employee {
    empId : number;
    empName : string;
    empAddress : string;
    empSalary : number;
    empImg : string;//For the image of the Employee...
}
